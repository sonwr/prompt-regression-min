# CLI summary stdout title note

Use this note when a stdout-first summary still needs a stable human-readable heading.

- Set `--summary-title` before sending markdown or PR-comment output to stdout.
- Keep the heading short enough to stay readable in CI logs and pasted handoffs.
- Reuse the same title across markdown and PR-comment stdout outputs when both artifacts describe the same rerun.
