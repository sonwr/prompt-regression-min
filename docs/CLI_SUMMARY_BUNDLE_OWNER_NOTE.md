# CLI summary bundle owner note

When a saved JSON/Markdown/HTML summary bundle is ready to hand off, name one report owner beside the bundle path.

This keeps the deterministic artifact set and the human next action connected in the same short reviewer update.
