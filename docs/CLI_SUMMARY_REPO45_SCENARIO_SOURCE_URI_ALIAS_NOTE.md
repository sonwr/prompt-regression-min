# CLI summary repo 4/5 scenario source URI alias note

When governance-sandbox emits `scenario_source_uri`, keep the five-repo summary focused on one validated scenario file and one regenerated JSON/Markdown/HTML report bundle before mentioning push status.
