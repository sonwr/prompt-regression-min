# CLI summary basename note

Use one shared basename when the JSON, Markdown, and HTML summary artifacts should reopen as a single review bundle.

## Rule

- Keep one report basename visible in reviewer handoff copy.
- Reopen the JSON summary first, then the Markdown/HTML pair from the same basename.
- Treat basename stability as part of the summary contract, not a cosmetic detail.
