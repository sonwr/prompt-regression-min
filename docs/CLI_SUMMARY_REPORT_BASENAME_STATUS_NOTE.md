# CLI Summary Report Basename Status Note

When a saved summary trio is already handoff-ready, keep the report basename visible in the same short status line.
That makes it easier to reconnect stdout guidance with the saved JSON, Markdown, and HTML artifacts without reopening the full command history.
