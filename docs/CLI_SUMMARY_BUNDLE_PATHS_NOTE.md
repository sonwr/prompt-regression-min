# CLI summary bundle paths note

Use this note when a `prompt-regression-min` run already emits summary artifacts and you want the handoff to stay easy to replay.

Keep two paths visible together:

- the machine-readable summary JSON path for downstream automation, and
- the reviewer-facing summary markdown path for human inspection.

If a run produces both artifacts, name both before widening the report surface or PR-comment wording.
