# CLI summary queue validate output-slug note

Keep one honest maintainer loop: pick the reviewer queue, rerun the validation command, and point the saved JSON/Markdown/HTML bundle at one shared output slug before posting a short status line.
