# CLI summary repo45 scenario source short alias note

When repo 4/5 progress summaries mention governance-sandbox scenario-file proof, keep the replay wording compatible with `scenario_src` and `source_href` so the saved summary still points at the original scenario source path or URL without expanding the one-line status format.
