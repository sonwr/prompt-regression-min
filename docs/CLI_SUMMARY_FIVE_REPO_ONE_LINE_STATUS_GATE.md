# CLI summary five-repo one-line status gate

For five-repo cron updates, keep each repo line limited to **repo name -> change state -> validation result -> commit/push status** so the short report stays auditable and honest.
