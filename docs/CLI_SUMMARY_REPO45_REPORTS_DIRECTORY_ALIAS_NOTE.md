# CLI summary repo-4/repo-5 reports_directory alias note

When the governance-sandbox phase-one pass names the generated bundle location, `reports_directory` is an acceptable scenario-file alias for the report output directory. Keep the summary honest by pairing that alias with the same validation rerun before any commit/push claim.
