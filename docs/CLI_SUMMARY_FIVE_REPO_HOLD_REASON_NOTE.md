# CLI summary five-repo hold-reason note

In the five-line cron report, keep each repo line short but explicit:
- repo name
- changed or unchanged
- validation passed or failed
- commit/push done, or the concrete hold reason

Do not blur `not pushed` with `validation failed` or `left dirty on purpose`.
