# CLI summary report priority-label note

When a saved summary bundle also exposes reviewer queue metadata, keep the priority label compact and reproducible.

- Reuse the same priority label across stdout, JSON, Markdown, and HTML.
- Keep the winning queue label visible beside the shared report basename.
- Prefer one short reviewer-facing status line over separate bundle-only wording.
