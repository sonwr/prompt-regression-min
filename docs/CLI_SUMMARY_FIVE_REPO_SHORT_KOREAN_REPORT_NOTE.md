# CLI summary five-repo short Korean report note

When a cron run touches five active repositories, keep the user-facing progress report in Korean and limit it to exactly one short plain-text line per repository.

Each line should keep the same order: repository name, change state, validation result, and commit/push status or hold reason.
