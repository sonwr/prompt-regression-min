# CLI summary quiet status bundle note

When the CLI runs with `--quiet`, keep one saved markdown bundle and one short honest status line so reviewers can reopen the artifact without re-reading stdout noise.
