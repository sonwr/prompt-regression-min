# CLI summary repo-4/repo-5 scenario report replay lane note

Keep the short five-repo status honest:

- repo 4 stays active when its validator-backed preset/docs slice moves,
- repo 5 stays phase-one-first with one scenario file replayed into one JSON/Markdown/HTML report bundle,
- push only after the touched repo reruns green validation.
