# CLI summary rerun command note

When a saved regression summary bundle is ready to share, keep the exact rerun command beside the JSON/Markdown/HTML artifact paths so reviewers can reproduce the same comparison without reconstructing flags by hand.
