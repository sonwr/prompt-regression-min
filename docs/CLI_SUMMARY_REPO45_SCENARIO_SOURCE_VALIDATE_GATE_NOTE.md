# CLI summary repo-4/repo-5 scenario-source validate gate note

When governance-sandbox adds short scenario-source aliases like `scenario_src` or `source_href`, keep the five-line status summary honest.

Rerun the smallest repo-4 and repo-5 validation commands before you write commit/push status so the alias proof and the short report stay coupled.
