# CLI summary one bundle note

Use this note when one regression-summary run should save **JSON, Markdown, and HTML artifacts together** while keeping the same status line easy to reuse in CI, PR comments, or reviewer handoff text.

Keep the bundle wording small and repeatable:

- one run
- one status line
- one shared JSON/Markdown/HTML artifact bundle
- one owner-ready handoff sentence

This keeps machine-readable output and reviewer-facing summaries aligned.
