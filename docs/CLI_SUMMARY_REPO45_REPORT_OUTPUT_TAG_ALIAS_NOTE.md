# CLI summary repo-4/repo-5 report-output-tag alias note

When the five-repo loop summarizes repo 4 and repo 5, the same governance-sandbox phase-one status can name the regenerated JSON/Markdown/HTML bundle with `report_output_tag` and still stay inside one short validation-backed line.

Keep commit/push wording gated on a fresh local validation rerun.
