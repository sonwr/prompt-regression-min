# CLI summary report output slug trio note

Use one `--output-slug` when a single regression run should emit JSON, Markdown, and HTML summary artifacts under the same basename.

That keeps reviewer handoff, saved artifact paths, and any short status line anchored to one deterministic bundle name.
