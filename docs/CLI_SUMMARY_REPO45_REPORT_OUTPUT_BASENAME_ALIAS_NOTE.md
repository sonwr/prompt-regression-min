# CLI_SUMMARY_REPO45_REPORT_OUTPUT_BASENAME_ALIAS_NOTE

When a five-repo status line mentions governance-sandbox report bundle naming, `report_output_basename` is acceptable shorthand for the shared JSON/Markdown/HTML artifact basename.

Keep the summary honest:
- only report validated bundle names
- keep repo 4 and repo 5 visible in the same pass
- keep the final handoff to exactly one line per repo
