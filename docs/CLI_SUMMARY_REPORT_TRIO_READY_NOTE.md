# CLI summary report trio ready note

When one regression review already saves JSON, Markdown, and HTML summary artifacts together, call it ready only if the trio shares one stable basename and one rerun command.

Use this as a compact handoff for reviewer updates that need the machine-readable JSON, the readable markdown memo, and the HTML report in one bundle.
