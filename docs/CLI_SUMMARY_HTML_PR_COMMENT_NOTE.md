# CLI Summary HTML + PR Comment Note

When one rerun needs both a browser-friendly artifact and a paste-ready reviewer note, keep `--summary-html` and `--summary-pr-comment` generated from the same deterministic command.

Reviewer rule: one saved HTML summary for visual reopen, one PR-comment summary for human triage, same run, same ids, same status.
