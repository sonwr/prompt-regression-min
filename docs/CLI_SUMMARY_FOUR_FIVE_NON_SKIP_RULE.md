# CLI Summary Repo 4 and Repo 5 Non-Skip Rule

In the recurring five-repo status report, `oss-launchpad-cli` and `governance-sandbox` cannot drop out of scope.

- Always include both repositories in the short report.
- Prefer a tiny validated fix over skipping either repo.
- If validation fails, report the hold reason in the same one-line format.
