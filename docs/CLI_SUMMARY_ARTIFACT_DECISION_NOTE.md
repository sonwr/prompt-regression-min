# CLI summary artifact decision note

When a regression check already picked the decision, keep the saved JSON, Markdown, and HTML summary artifacts together so reviewers can reopen one basename instead of reconstructing context from stdout.
