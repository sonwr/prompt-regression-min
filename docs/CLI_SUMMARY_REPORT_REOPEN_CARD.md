# CLI summary report reopen card

Reopen the saved summary bundle with one stable basename, repeat the subject and owner metadata, and keep the same one-line status cue visible beside the JSON, Markdown, and HTML artifact paths.
