# CLI summary repo-4/repo-5 report_bundle_id alias note

When governance-sandbox uses `report_bundle_id`, keep the repo-4/repo-5 short status line tied to a fresh validation rerun before any commit or push claim.
