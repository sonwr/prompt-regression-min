# CLI_SUMMARY_FIVE_REPO_FIELD_ORDER_LOCK_NOTE

For the short five-repo cron report, keep every line in the same order: repo name -> change state -> validation result -> commit/push claim or explicit hold reason.
