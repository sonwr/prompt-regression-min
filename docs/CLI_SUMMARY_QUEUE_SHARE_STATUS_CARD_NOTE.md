# CLI Summary Queue Share Status Card Note

When shortening a regression pass for a reviewer, keep one compact status card that still shows:

- the active follow-up queue
- queue share
- active-case rate
- source-case rate

That keeps the short report readable without hiding which queue actually dominates the remaining work.
