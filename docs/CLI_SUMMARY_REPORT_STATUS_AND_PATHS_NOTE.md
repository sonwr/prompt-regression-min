# CLI summary report status and paths note

When sharing one regression-summary result, keep the pass/fail status line and the saved JSON/Markdown/HTML artifact paths together.

This keeps reviewers from reopening stdout alone when the real proof lives in the saved report bundle.
