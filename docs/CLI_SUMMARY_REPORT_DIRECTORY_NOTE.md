# CLI summary report directory note

When one review run saves JSON, Markdown, and HTML together, keep the shared report directory visible beside the status line so reviewers can reopen the whole bundle without guessing paths.
