# CLI summary validate artifact loop note

Keep the smallest proof loop tight: regenerate one summary artifact from one CLI fixture, verify the artifact still matches the expected story, then push only after that local check passes.
