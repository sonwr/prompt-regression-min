# CLI summary report note

For the smallest believable regression-review loop, keep one command, one summary table, and one baseline/output artifact pair visible together.

Recommended order:

1. run the CLI against one stable case set,
2. save or compare one JSON/text summary artifact,
3. call out pass-rate drift in one reviewer-facing sentence,
4. widen only after the compact summary stays reproducible.
