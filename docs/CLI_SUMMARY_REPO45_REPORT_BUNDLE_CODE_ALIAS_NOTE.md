# CLI summary repo45 report bundle code alias note

When repo 5 reports a scenario-file-first win, short status lines can mention `report_bundle_code` as the shared basename alias for the regenerated JSON/Markdown/HTML governance report bundle, but only after validation passes.
