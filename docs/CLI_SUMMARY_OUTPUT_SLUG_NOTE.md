# CLI summary output slug note

Use one output slug when you want the saved JSON, Markdown, and HTML summary artifacts to share predictable filenames without repeating the basename in multiple flags.

This keeps reviewer handoff copy short while preserving one reproducible artifact bundle.
