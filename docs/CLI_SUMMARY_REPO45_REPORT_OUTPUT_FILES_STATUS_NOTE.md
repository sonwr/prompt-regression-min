# CLI_SUMMARY_REPO45_REPORT_OUTPUT_FILES_STATUS_NOTE

Use this note when the five-line cron summary should mention repo 4 validation progress and repo 5 scenario/report progress while the regenerated governance artifact trio is exposed via `report.outputs.files`.

Keep the short line honest:
- repo 4 still reports validation-first status,
- repo 5 still reports one scenario-file replay plus one regenerated report bundle,
- `report.outputs.files` is just the saved artifact-path cue for the same JSON / Markdown / HTML bundle.
