# CLI summary queue mix summary note

Keep the queue-mix summary visible when the saved JSON/Markdown/HTML bundle needs one compact explanation of how the follow-up work is distributed across reviewer queues.
