# CLI summary validate then reopen bundle note

Validate the smallest deterministic summary slice first, then reopen the saved JSON / Markdown / HTML bundle for review.

That keeps reviewer handoff tied to a fresh passing run instead of reopening stale artifacts after a docs-only edit.
