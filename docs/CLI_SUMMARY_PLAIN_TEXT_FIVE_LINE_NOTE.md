# CLI_SUMMARY_PLAIN_TEXT_FIVE_LINE_NOTE

Keep the cron-facing handoff plain text and exact: one short Korean line per repo, five total lines, and each line names change state, validation result, plus commit/push status or the hold reason.

That keeps saved summary artifacts machine-friendly while the user-facing update stays minimal and honest.
