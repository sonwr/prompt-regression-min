# CLI summary five-repo commit hold reason note

In five-repo cron updates, keep each one-line status honest: name the repo, whether something changed, validation result, and either commit/push status or the exact hold reason. If validation fails or a repo stays intentionally unshipped, say that explicitly instead of implying a push-ready state.
