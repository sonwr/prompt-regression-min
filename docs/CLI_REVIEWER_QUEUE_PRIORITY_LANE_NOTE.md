# CLI reviewer queue priority-lane note

When the reviewer queue is non-empty, keep the next handoff scoped to the highest-priority queue key plus its IDs before expanding into broader release commentary. This preserves a small, reproducible repair lane for follow-up reruns.
