# CLI summary one-line status note

Keep the maintainer-facing progress update to one short line per repo that states the repository name, whether a change landed, whether validation passed, and whether commit/push happened or is on hold.
