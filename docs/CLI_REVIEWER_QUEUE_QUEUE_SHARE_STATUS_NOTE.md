# CLI reviewer queue queue-share status note

Keep the reviewer queue status lane compact: when follow-up work is grouped, show the queue share beside the active-case rate so owners can see which lane dominates the next pass first.
