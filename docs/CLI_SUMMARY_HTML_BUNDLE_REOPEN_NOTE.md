# CLI summary HTML bundle reopen note

Keep one saved JSON, Markdown, and HTML summary bundle reopenable from the same reviewer handoff.

## Quick check
- The status line names the shared summary bundle.
- Reviewers can reopen JSON, Markdown, and HTML artifacts without inventing new filenames.
