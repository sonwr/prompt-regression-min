# CLI summary report paths note

When a regression run writes JSON, Markdown, and HTML summaries, keep the saved report paths visible in the same handoff line.
That keeps reviewer follow-up focused on one reproducible artifact bundle instead of a vague status update.
