# CLI summary short repo status line note

When a maintainer needs the smallest honest repo update, keep four fields in the same line:

1. repo name,
2. changed/unchanged state,
3. validation pass/fail,
4. commit/push state or hold reason.

Use this after local validation so a one-line progress report still reflects a real checked slice instead of vague activity.
