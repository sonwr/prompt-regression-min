# CLI_SUMMARY_REPO45_SHORT_KR_STATUS_NOTE

For the user-facing cron update, keep repo 4 (`oss-launchpad-cli`) and repo 5 (`governance-sandbox`) visible in Korean.
The final summary still stays exactly five short lines, with one line per repo.
Each repo-4/repo-5 line should name change state, validation result, and commit/push status or hold reason.
