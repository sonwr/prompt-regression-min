# CLI summary five-repo fixed order note

Keep the short cron report in the same fixed order every run:

1. `openclaw-codex-pm-skills`
2. `prompt-regression-min`
3. `awesome-agent-skills-ko`
4. `oss-launchpad-cli`
5. `governance-sandbox`

That keeps one-line status scans comparable across runs and makes it obvious when repo 4 and repo 5 were not skipped.
