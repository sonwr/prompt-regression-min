# CLI summary repo45 stakeholder_types wrapper note

Keep the repo-4/repo-5 short status lane honest when repo 5 accepts wrapper-shaped scenario files that still carry stakeholder preset maps as `stakeholder_types`.

Short-report rule stays unchanged:

- one line per repo,
- validation result must be explicit,
- commit/push claims only after the validator passes.
