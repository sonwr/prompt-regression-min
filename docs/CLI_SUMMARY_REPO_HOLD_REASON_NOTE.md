# CLI summary repo hold-reason note

When a short repo status line cannot move to commit/push, say why. A compact line should still name the validation result and the concrete hold reason so reviewers can tell whether the repo is waiting on tests, validator recovery, or a deliberate no-change pass.
