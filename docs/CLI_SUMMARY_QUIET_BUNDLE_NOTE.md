# CLI summary quiet bundle note

Use `--quiet` when stdout should stay machine-friendly, but still save JSON/Markdown/HTML artifacts for reviewer handoff. Quiet terminal output and explicit report files can coexist without changing the scoring contract.
