# CLI summary validation gate owner note

Keep one passing local validation command visible beside the saved summary bundle before commit/push. That keeps owner-facing status language honest when JSON, Markdown, and HTML artifacts are regenerated from the same deterministic run.
