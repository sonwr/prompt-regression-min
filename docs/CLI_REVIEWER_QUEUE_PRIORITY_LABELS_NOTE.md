# CLI reviewer queue priority labels note

When `prompt-regression-min` emits reviewer-queue metadata, keep the handoff copy stable: name the winning queue first, keep the printed priority label visible, and pair it with the saved JSON/Markdown/HTML summary bundle when one exists.

That keeps short progress notes readable without reopening the full payload just to recover the queue order.
