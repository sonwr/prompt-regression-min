# CLI summary repo-4/repo-5 report output ref alias note

Keep the short five-repo status honest when governance-sandbox exposes the shared report basename as `report_output_ref`: validate the same scenario-file plus JSON/Markdown/HTML bundle first, then report repo 4 and repo 5 in one short line each.
