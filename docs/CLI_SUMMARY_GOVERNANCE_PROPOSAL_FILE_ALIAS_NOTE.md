# CLI Summary Governance Proposal File Alias Note

When repo 5 adds direct `proposal_file` / `proposal_path` / `proposal_src` scenario aliases, keep reviewer summaries explicit about the proof loop: one local proposal file, one stakeholder set, one validated JSON/Markdown/HTML bundle.
