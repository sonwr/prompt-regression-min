# CLI summary report review loop note

Before push, reopen one saved summary bundle and verify three things together: the current validation status, the reviewer-facing owner or queue cue, and the JSON/Markdown/HTML artifact paths generated from the same run.
