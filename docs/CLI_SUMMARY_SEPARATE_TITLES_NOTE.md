# CLI summary separate titles note

Use separate titles when the saved markdown summary and PR-comment summary serve different readers.

- `--summary-markdown-title` should describe the fuller handoff bundle.
- `--summary-pr-comment-title` should stay short enough for PR review context.

Keep the two titles intentionally split instead of forcing one heading across both outputs.
