# CLI_SUMMARY_REPO45_SHORT_LINE_PRESENCE_GATE

Keep the five-repo cron summary compact, but never let repo 4 or repo 5 disappear behind brevity. Each still needs its own short status line with validation outcome plus commit/push state or hold reason.
