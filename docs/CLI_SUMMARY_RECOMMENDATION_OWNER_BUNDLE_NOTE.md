# CLI summary recommendation owner bundle note

Keep the saved summary recommendation, named owner, and shared JSON/Markdown/HTML bundle visible in one short handoff so reviewer follow-up does not lose the decision or artifact path.
