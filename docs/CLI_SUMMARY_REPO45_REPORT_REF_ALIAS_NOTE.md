# CLI summary repo-4/repo-5 report-ref alias note

Keep the repo-4/repo-5 cron line honest: when repo 5 accepts `report_bundle_ref` from a JSON/YAML scenario file, rerun validation before any commit/push claim.
If validation fails, keep the one-line report on an explicit hold instead of implying that the report bundle is ready.
