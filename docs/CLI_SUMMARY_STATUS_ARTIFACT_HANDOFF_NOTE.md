# CLI summary status artifact handoff note

When a run saves summary artifacts, keep one reviewer-facing handoff sentence that names both:

- the stdout status line, and
- the shared JSON / Markdown / HTML bundle.

That keeps quick terminal proof and reusable artifact proof aligned in the same regression-review loop.
