# CLI summary discover-then-push note

When a summary or reviewer-queue slice touches multiple rendering surfaces, prefer `python3 -m unittest discover -s tests` before push so the CLI payload, stdout wording, and saved JSON/Markdown/HTML artifacts stay green together.
