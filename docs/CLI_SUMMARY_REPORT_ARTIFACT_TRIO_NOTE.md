# CLI summary report artifact trio note

When a summary workflow emits report artifacts, keep the handoff compact and explicit:

- name the JSON/Markdown/HTML trio together,
- keep the shared basename visible,
- and mention whether stdout, bundle files, or both were produced.

This keeps reviewer-facing status lines short while preserving replayable report evidence.
