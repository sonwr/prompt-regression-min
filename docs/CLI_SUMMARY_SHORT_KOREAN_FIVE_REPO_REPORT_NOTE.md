# CLI summary short Korean five-repo report note

When the maintainer loop covers five active repositories, emit exactly one short Korean status line per repository and keep each line limited to repository name, change status, validation result, and commit/push state or hold reason.
