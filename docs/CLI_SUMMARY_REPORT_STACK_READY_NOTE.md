# CLI summary report stack ready note

When one run saves JSON, Markdown, and HTML together, the reviewer-facing status line should say the report stack is ready only after all three files exist and the same basename still matches.

Compact check:

- one command
- one basename
- one JSON/Markdown/HTML stack
- one short status line
