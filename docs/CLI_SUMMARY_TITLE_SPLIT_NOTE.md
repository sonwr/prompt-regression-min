# CLI summary title split note

Use separate markdown and PR-comment titles when the same regression run needs both a durable summary artifact and a reviewer-facing handoff. Keep the markdown title report-like, keep the PR-comment title action-oriented, and validate both paths in one proof loop.
