# CLI summary repo 4/5 validate-hold note

In the five-repo short report, keep repo 4 and repo 5 visible even when they do not ship: show one validated change when possible, otherwise keep the one-line hold reason attached to the same repo line before any commit/push claim.
