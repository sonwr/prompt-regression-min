# CLI_SUMMARY_REVIEWER_QUEUE_HUD_NOTE

When you export a markdown or PR-comment summary, keep the reviewer queue handoff scannable: one short title, one queue-mix line, and one next-focus line that tells reviewers what to touch first.
