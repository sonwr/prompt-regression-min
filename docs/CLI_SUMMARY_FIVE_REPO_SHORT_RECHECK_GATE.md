# CLI summary five-repo short recheck gate

Before posting the five-line cron report, rerun one explicit local validation command.

Each repo line should still name, in order:

1. repository name
2. change state
3. validation result
4. commit/push result or hold reason

Use this note when you need the shortest reminder that the final report stays exactly five lines and that commit/push claims only follow a passing rerun.
