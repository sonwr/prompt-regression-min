# CLI summary repo45 proposal markdown path note

When repo 5 expands scenario-file input, short status summaries should call out that `proposal_markdown_path` / `proposal_md_path` can keep one markdown proposal beside the JSON/YAML scenario. repo 4 stays active in the same loop, and commit/push claims still wait behind passing validation.
