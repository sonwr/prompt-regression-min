# CLI summary report trio start

Generate the JSON, Markdown, and HTML summary artifacts together when the reviewer needs one replayable bundle. Keep owner, subject, status, and artifact paths visible in the same handoff.
