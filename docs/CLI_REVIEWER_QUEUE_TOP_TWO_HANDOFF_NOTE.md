# CLI reviewer queue top-two handoff note

Use the reviewer queue output to surface the first two follow-up lanes without bloating the PR comment.

- Lead with the highest-priority queue first.
- Keep the second lane visible so reviewers can see the immediate backup action.
- Reuse the same ordering in markdown summaries, PR comments, and JSON handoff payloads.
