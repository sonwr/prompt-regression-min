# CLI Summary Quiet JSON Bundle Note

Use this note when a CI log should stay quiet on stdout while still saving a JSON summary artifact for downstream review.

Recommended pairing:
- enable quiet stdout for log hygiene
- save the JSON summary to a predictable artifact path
- mention that quiet stdout does not replace artifact review
