# CLI summary five-repo short status note

When one automation pass touches five active repositories, keep the user-facing summary to one honest status line per repository after validation and before any push claim.
