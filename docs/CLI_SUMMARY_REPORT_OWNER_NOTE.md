# CLI summary report owner note

When a saved summary artifact needs an explicit human owner, keep the owner line in the report bundle instead of burying it in the PR comment.

A good narrow handoff pairs:
- the selected reviewer queue,
- the shared JSON/Markdown/HTML summary bundle,
- and one owner name or team responsible for the next rerun.
