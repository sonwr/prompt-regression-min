# CLI summary report owner + queue status note

Use one short reviewer-facing status line when a saved summary bundle is already generated:

- keep the named **owner** visible
- keep the selected **next-review queue** visible
- keep the saved JSON / Markdown / HTML bundle status visible

This is the compact handoff to use after validation passes and before commit/push.
