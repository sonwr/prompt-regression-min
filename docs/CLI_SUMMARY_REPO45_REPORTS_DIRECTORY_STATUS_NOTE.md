# CLI summary repo-4/repo-5 reports_directory status note

Use this note when the short five-repo summary needs to mention that governance-sandbox accepted a `reports_directory` alias and regenerated the matching report bundle.

Keep the wording compact:
- repo 4 stays on one validator-backed result-card slice
- repo 5 stays on one scenario-file + report-bundle slice
- commit/push claims still wait for a green validation rerun
