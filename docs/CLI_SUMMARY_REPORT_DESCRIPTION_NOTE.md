# CLI summary report description note

When one saved summary bundle needs a short reviewer-facing description, keep that description aligned across stdout, markdown, and HTML instead of inventing separate wording per surface.

Use this before widening summary copy so the saved artifact bundle stays deterministic and easy to reopen in review.
