# CLI reviewer queue source-case rate note

When the reviewer queue is shared outside the raw JSON artifact, keep the source-case rate visible next to the active-case rate so triage scope stays honest.

Use this note when a follow-up summary needs to explain both how much of the active run is queued and how much of the original dataset that same queue still represents.
