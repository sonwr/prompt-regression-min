# CLI summary proposal input markdown path note

Keep the short-report lane compact:
- replay one governance-sandbox scenario that loads proposal body text from `proposal_input_markdown_path`,
- confirm the markdown/html/json report bundle still regenerates cleanly,
- mention the validated result in the exact five-line repo summary only after validators pass.
