# CLI Summary Five-Repo Compact Status Gate

When a recurring maintainer pass spans five repos, keep the user-facing summary brutally compact.

Checklist:
- exactly one line per repo,
- include change status,
- include validation pass/fail,
- include commit/push result or the hold reason.

Use this after the local validation step so the saved summary stays short without hiding whether each repo actually moved.
