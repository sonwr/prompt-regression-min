# Report Queue Mix Note

Keep the reviewer queue readable by showing the queue mix next to the current report bundle handoff. A small validated pass should name the active queue split, the report bundle being reviewed, and whether a rerun is needed before push.
