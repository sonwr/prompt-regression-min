# CLI Summary Report Decision Queue Note

When a run is ready for review, keep the handoff order compact and deterministic:

1. print the summary status line,
2. point to the shared JSON/Markdown/HTML report bundle,
3. name the reviewer queue or owner.

That keeps the decision queue anchored to the same saved summary artifacts instead of ad-hoc copy.
