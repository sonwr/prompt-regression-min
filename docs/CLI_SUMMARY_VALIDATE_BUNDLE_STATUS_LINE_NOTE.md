# CLI_SUMMARY_VALIDATE_BUNDLE_STATUS_LINE_NOTE

Keep the shortest saved-summary status line honest: show whether artifacts changed, name the validation command that passed, and only then present the bundle as review-ready.
