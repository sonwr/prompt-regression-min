# CLI Summary Quiet Dual Title Note

Use this when one regression run needs quiet stdout but still saves different markdown and PR-comment headings.

Keep the run narrow: one dataset, one status, one saved bundle, two explicit reviewer-facing titles.
