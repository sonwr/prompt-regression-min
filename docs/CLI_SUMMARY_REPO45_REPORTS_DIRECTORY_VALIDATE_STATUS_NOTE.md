# CLI summary repo-4/repo-5 reports_directory validate-status note

When the five-repo cron summary mentions `reports_directory` for `oss-launchpad-cli` and `governance-sandbox`, keep the same short line tied to a fresh validation rerun and an explicit commit/push or hold status.
