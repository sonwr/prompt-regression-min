# CLI summary status owner note

When posting a short reviewer-facing summary status line, keep the visible pass/fail or hold signal paired with the named report owner so the same saved bundle still has one explicit follow-up target.
