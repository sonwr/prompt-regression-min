# CLI_SUMMARY_REOPEN_COMMAND_NOTE

When a summary bundle is already generated, keep one short rerun command beside the artifact paths so reviewers can reopen the exact same comparison without reconstructing flags from memory.

Recommended trio:

- summary path,
- PR-comment path,
- rerun command.

Use this note when tightening the smallest validated review loop.
