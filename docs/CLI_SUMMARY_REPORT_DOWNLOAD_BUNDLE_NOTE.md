# CLI summary report download bundle note

Keep one validated summary bundle ready for reviewer download before widening follow-up formatting changes.

- Validate the CLI output first.
- Keep JSON and Markdown artifacts under one shared basename.
- Reopen the saved bundle instead of inventing a second handoff path.
- Push only after the documented bundle flow still passes tests.
