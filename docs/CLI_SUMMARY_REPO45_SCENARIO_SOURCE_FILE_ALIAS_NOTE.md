# CLI summary repo-4/repo-5 scenario_source_file alias note

When the repo-4/repo-5 status lane references governance-sandbox phase-one inputs, allow `scenario_source_file` as the same file-first alias family as `source_file` and `source_path`. Keep the saved summary honest: one scenario file in, one validated JSON/Markdown/HTML report bundle out.
