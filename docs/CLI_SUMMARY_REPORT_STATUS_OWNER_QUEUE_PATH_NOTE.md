# CLI Summary Report Status Owner Queue Path Note

When a saved summary bundle is ready to hand off, keep four things visible in one compact line:

- current status
- named owner
- active reviewer queue
- saved artifact paths

That combination makes the markdown summary easier to reopen without forcing readers to reconstruct who owns the rerun or which bundle to inspect first.
