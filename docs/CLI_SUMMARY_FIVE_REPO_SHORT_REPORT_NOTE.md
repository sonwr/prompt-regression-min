# CLI summary five-repo short report note

When reporting the active-repo sweep, keep the user-facing update to exactly five lines and keep each line limited to repository name, changed/unchanged status, validation result, and commit/push outcome.
