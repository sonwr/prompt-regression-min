# CLI summary stdout/bundle pair note

Keep one stdout recommendation line paired with the same saved JSON/Markdown/HTML bundle.

If you shorten the console output, make sure the artifact bundle still carries the same decision context.
