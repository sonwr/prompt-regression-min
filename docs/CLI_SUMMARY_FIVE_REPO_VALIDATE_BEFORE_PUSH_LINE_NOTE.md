# CLI summary five-repo validate-before-push line note

In a five-line cron report, each repo line should mention commit or push only after the repo-specific validation rerun has passed; otherwise keep the line on an explicit hold reason.
