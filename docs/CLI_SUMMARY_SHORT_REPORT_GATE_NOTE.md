# CLI summary short report gate note

When posting a short progress line from a saved summary bundle, keep this order stable:

1. status,
2. validation result,
3. bundle or reopen path.

The point is to make tiny reviewer updates stay honest, deterministic, and easy to compare across reruns.
