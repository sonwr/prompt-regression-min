# CLI summary markdown status + owner note

When writing the first markdown summary line, keep the status signal and the owner in the same sentence so the follow-up lane is obvious without opening the JSON artifact.
