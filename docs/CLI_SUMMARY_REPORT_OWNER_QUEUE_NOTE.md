# CLI summary report owner + queue note

When a saved summary bundle is ready for human review, keep four things visible in one short handoff:

- the current status line
- the named report owner
- the active reviewer queue or next-focus queue
- the shared JSON/Markdown/HTML bundle path

That keeps rerun ownership, queue priority, and artifact reopening aligned without forcing reviewers to open the raw JSON first.
