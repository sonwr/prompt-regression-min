# CLI summary shared basename note

Keep one stable report basename when saving JSON, Markdown, and HTML summary artifacts from the same run.
That makes CI links, PR comments, and reviewer reopen steps point at one recognizable bundle instead of three unrelated filenames.
