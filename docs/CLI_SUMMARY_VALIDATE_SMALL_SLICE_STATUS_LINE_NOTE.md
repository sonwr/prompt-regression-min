# CLI summary validate-small-slice status line note

Use one compact maintainer line after a small doc or summary change:

- change landed or held,
- smallest validation command,
- pass/fail result,
- commit/push only after green.

This keeps five-repo cron updates short without hiding the validation gate.
