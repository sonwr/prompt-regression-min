# Governance scenario report_output_code check

Use `report_output_code` as a compact basename alias when you need one more replayable scenario-file -> markdown/html/json report bundle check without widening the scope beyond the current phase-one proof.
