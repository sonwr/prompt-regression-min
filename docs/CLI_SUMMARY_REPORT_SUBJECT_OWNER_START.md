# CLI summary report subject + owner start

When a saved summary bundle needs reviewer-ready metadata, keep the subject and owner visible together from the first command example.

Minimum proof:
- one summary command that writes the artifact bundle
- one explicit report subject
- one explicit report owner
- one matching README or review note that points to the saved bundle
