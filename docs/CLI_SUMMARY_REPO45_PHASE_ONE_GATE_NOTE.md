# CLI summary repo45 phase-one gate note

Keep repo 4 and repo 5 visible as the same mandatory pair in the five-line cron summary, while repo 5 still stays phase-one-first: scenario-file input first and JSON/Markdown/HTML report bundle output second.

Commit or push only after the validation command stays green for the repo named in that one-line status.
