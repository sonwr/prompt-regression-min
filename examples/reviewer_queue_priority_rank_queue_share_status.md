# Reviewer queue priority-rank queue-share status

Use one line that keeps the winner, queue-share percentage, and next action together.

Example: `fix_regressions leads at 50.00% queue share; rerun the regression bundle first.`
