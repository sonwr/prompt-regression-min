# Reviewer queue HTML report download gate

Before posting a short reviewer update, confirm the HTML-ready queue still points to a shared report bundle that can be reopened without guessing filenames.

Fast gate:
- name the active queue,
- reopen the shared JSON/Markdown/HTML bundle,
- only post the handoff once the HTML report path is still deterministic.
