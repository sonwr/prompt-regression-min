# Reviewer queue progress title split

Use one short progress headline for chat or PR commentary, then keep the saved summary bundle title stable and artifact-oriented.

This avoids mixing transient status wording with filenames or report metadata that should stay reproducible across reruns.
