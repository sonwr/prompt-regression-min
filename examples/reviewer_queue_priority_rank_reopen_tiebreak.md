# reviewer_queue_priority_rank_reopen_tiebreak

If two reviewer lanes share the same priority rank, reopen the one whose saved artifact bundle still includes the clearest JSON + Markdown + HTML proof for the next reviewer handoff.
