# Reviewer queue HTML bundle reopen note

Use this one-line handoff when the HTML report, JSON artifact, and markdown summary should be reopened together for the same reviewer queue:

> Reopen the active reviewer queue with the shared JSON, markdown, and HTML bundle before posting the final handoff.
