# Summary stdin bundle note

Use this note when a prompt-regression summary is generated from stdin-fed fixtures but still needs one stable bundle name for JSON, Markdown, and PR-comment outputs.

Keep the slice small:

1. one stdin-fed input set,
2. one explicit report basename,
3. one validation pass that proves the human-readable and machine-readable outputs still match.
