# Reviewer queue validate-then-share note

Use one compact sequence before posting a reviewer-facing update:

1. rerun the smallest deterministic validation command,
2. regenerate the shared summary bundle,
3. post one short owner/status line that names the bundle.

This keeps queue choice, proof, and communication aligned.
