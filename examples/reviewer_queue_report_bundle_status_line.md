# Reviewer queue report bundle status line

Use one short handoff line when the winning reviewer queue and the saved summary bundle are both ready to mention again.

Template:

`Top follow-up: <queue> -> <reason>. Shared bundle: <basename>. Status: JSON/Markdown/HTML artifacts regenerated and gate still PASS.`
