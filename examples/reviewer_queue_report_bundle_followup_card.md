# Reviewer queue report bundle follow-up card

Use this when the next reviewer focus is already selected and you only need one short follow-up sentence that keeps the queue plus the shared JSON / Markdown / HTML bundle together.
