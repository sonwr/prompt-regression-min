# Reviewer queue stable bundle gate

Use this quick gate before posting a "stable enough to review" queue update:

- the winning queue is still the top recommendation,
- the shared report bundle paths are known,
- the runner-up does not overtake on the latest rerun,
- the handoff sentence still names the next human action.
