# Reviewer queue priority-rank release handoff

Use this when one reviewer queue already has the clearest priority-rank lead and you need a short release-facing handoff.

Example line:

> Release review should start with the `billing` queue because it leads priority rank and still preserves the strongest source-case coverage among queued regressions.
