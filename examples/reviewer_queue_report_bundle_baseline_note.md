# Reviewer queue report bundle baseline note

Use this note when one reviewer queue already has a stable baseline and the follow-up needs to point back to the shared JSON, Markdown, and HTML report bundle.

Keep the wording short:
- name the active queue,
- mention that the baseline bundle is current,
- and point to the next reviewer action.
