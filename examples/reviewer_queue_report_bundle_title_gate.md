# Reviewer queue report bundle title gate

Use this before posting a short reviewer handoff for a saved report bundle.

- Confirm the winning reviewer queue is already stable enough to name.
- Reopen the shared JSON / Markdown / HTML bundle before trusting the title.
- Keep the visible title aligned with the same report basename used in artifacts.
- If the title promises approval, make sure the queue and pass-rate trend still support it.
