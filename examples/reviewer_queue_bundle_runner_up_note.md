# Reviewer queue bundle runner-up note

Use this when the current reviewer-queue winner is clear enough to lead the handoff, but the runner-up still matters for the next pass.

Template:
- Lead with the current queue winner.
- Name the runner-up in the same sentence when its queue share or source-case impact is still meaningful.
- Keep the shared JSON/Markdown/HTML bundle path in the same handoff so reviewers reopen one evidence set.
