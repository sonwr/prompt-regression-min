# Reviewer queue title and basename handoff

Use this note when one reviewer-facing queue summary already has a stable title and now needs the matching `--report-file-stem` or bundle basename to stay deterministic across JSON, Markdown, and HTML outputs.
