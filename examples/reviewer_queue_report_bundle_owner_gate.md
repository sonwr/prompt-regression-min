# Reviewer queue report bundle owner gate

Use this note when you need a compact owner-facing go/no-go check before reposting a saved reviewer queue bundle.

- Confirm the selected queue still leads for the same reason.
- Confirm the shared JSON/Markdown/HTML bundle paths still resolve from the same replay run.
- Post the short owner update only after the saved bundle and stdout status line still agree.
