# Reviewer queue report bundle start

Use this note when reviewers only need the narrowest restart path for one selected queue plus one shared JSON/Markdown/HTML summary bundle.

- reopen the chosen queue
- reopen the matching report bundle
- keep the status line scoped to the same saved artifacts
