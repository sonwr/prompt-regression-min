# Reviewer queue report bundle scope status line

Use this one-line handoff when a reviewer queue winner is real, but the update should stay explicitly tied to the shared JSON/Markdown/HTML report bundle instead of sounding release-wide.
