# summary report output files note

Use one shared basename when saving JSON, Markdown, and HTML summary artifacts so reviewers can reopen the same report bundle without guessing filenames.

Smallest reminder: keep `--summary-json`, `--summary-markdown`, and `--summary-report` paths visibly paired in the same handoff line.
