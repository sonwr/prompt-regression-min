# reviewer_queue_priority_rank_bundle_ready_post

Use this when the top-ranked reviewer queue is already clear and the shared artifact bundle is the only context you need to mention.

Template:

`Next focus: <priority label> is ready to post; reopen <shared report bundle> first so the queue winner stays tied to one reviewable JSON/Markdown/HTML bundle.`
