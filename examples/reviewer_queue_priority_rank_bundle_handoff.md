# Reviewer Queue Priority-Rank Bundle Handoff

Use one short line when reopening a saved summary bundle after queue selection:

- Name the winning queue key first.
- Include the exposed priority rank next.
- End with the shared JSON / Markdown / HTML bundle basename.

Example: `Next focus: fix_regressions (P1) -> reopen reviewer-queue-demo.{json,md,html}`
