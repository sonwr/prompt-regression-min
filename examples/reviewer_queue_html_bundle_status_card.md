# Reviewer Queue HTML Bundle Status Card

Use one short status card when the active reviewer queue already points to a shared JSON, Markdown, and HTML bundle. Name the queue winner, say the bundle is ready or blocked, and keep the wording tied to the same artifact set.
