# Reviewer queue priority-rank scope release note

Use this when the exposed priority-rank winner is real, but the release note still needs honest scope.

Template:

- Next reviewer focus: `<winner label>`.
- Scope: priority-rank lead is visible, but keep the runner-up or hold context if the margin is narrow.
- Artifact to reopen: JSON summary + markdown/PR comment generated from the same run.
