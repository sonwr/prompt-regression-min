# Reviewer Queue Release-Gate Bundle Scope Card

Use this when one reviewer queue already leads and you want a release-safe note that still points to the shared report bundle.

- Name the winning queue.
- Name the shared JSON/Markdown/HTML bundle to reopen.
- Keep the status scoped to the current release-review lane.
