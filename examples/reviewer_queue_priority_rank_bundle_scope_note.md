# Reviewer queue priority-rank bundle scope note

Keep the chosen priority rank, the reason it won, and the shared JSON/Markdown/HTML summary bundle in one short handoff so reviewers can reopen the same artifact stack without guessing scope.
