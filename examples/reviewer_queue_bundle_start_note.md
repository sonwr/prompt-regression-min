# reviewer queue bundle start note

Start with one selected reviewer queue and one saved report bundle.

Use this when you need a short handoff that keeps:

- the chosen reviewer lane,
- the saved JSON summary,
- the markdown summary, and
- the HTML review artifact

visible in the same sentence.
