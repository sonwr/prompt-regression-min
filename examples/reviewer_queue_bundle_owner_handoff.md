# Reviewer queue bundle owner handoff

Name the active reviewer queue first, then the shared report basename, then the saved JSON / Markdown / HTML artifacts so an owner can reopen the same bundle without guessing.
