Use one short reviewer line when the winner is already known and the important proof is just case volume:

`Priority queue stays docs-review (6/8 source cases, 2/8 candidate-only), reopen the shared JSON/Markdown/HTML bundle before widening scope.`
