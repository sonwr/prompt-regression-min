# reviewer_queue_filtered_scope_budget_note

Use this note when filtered-out scope dominates the reviewer queue and you need a short handoff that keeps the filtered-case budget visible.

- Mention the filtered-out ids or shard name.
- Say whether the filtered-out budget is exhausted or still has room.
- Keep the next action scoped to confirming filters before widening the queue.
