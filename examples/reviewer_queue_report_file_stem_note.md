# Reviewer queue report file-stem note

Use `--report-file-stem` when the JSON summary, markdown handoff, and HTML review page should reopen under the same reviewer-facing basename.

Quick check: run the same case set once, then confirm the three artifact paths share one file stem before posting the handoff note.
