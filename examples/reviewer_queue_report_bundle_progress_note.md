# Reviewer queue report bundle progress note

Use one small validated bundle when reporting progress:

- keep the queue summary tied to the same JSON/Markdown/HTML bundle
- mention the current owner or lane only when the bundle is already validated
- prefer one short progress post over multiple partial updates

This keeps progress updates compact and evidence-backed.
