# reviewer_queue_html_report_gate

Use this note before posting an HTML-oriented reviewer handoff.

- Confirm the current queue winner is still visible in summary JSON.
- Reopen the matching markdown/html bundle instead of trusting filenames alone.
- Post the reviewer sentence only when the queue lead and the reopened bundle still describe the same slice.
